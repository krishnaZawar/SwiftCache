HSET command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk write
        3. Invalid command name
        4. Only command name passed
        5. No field-value pairs passed
        6. Field-value pairs incomplete

HGET command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk fetch
        3. Invalid command name
        4. Only command name passed
        5. No field name passed

HGETALL command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Extra params passed

HKEYS command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Extra params passed

HDEL command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk delete
        3. Invalid command name
        4. Only command name passed
        5. No field name passed

HEXISTS command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk check
        3. Invalid command name
        4. Only command name passed
        5. No field name passed

HLEN command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Extra params passed

HSTRLEN command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk fetch
        3. Invalid command name
        4. Only command name passed
        5. No field name passed