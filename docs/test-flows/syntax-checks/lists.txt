LPUSH command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk insert
        3. Invalid command name
        4. Only command name passed
        5. No values passed

RPUSH command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk insert
        3. Invalid command name
        4. Only command name passed
        5. No values passed

LPUSHIDX command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Index not passed
        5. Index and value not passed
        6. Index is not an integer
        7. Extra params passed

LPOP command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Count not passed
        5. Count is not an integer
        6. Extra params passed

RPOP command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Count not passed
        5. Count is not an integer
        6. Extra params passed

LPOPIDX command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Index is not passed
        5. Index is not an integer
        6. Extra params passed

LLEN command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. Extra params passed

LINDEX command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk fetch
        3. Invalid command name
        4. Only command name passed
        5. Index not passed
        6. Index passed is not an integer

LRANGE command:
    Syntax check flows:
        1. Correct syntax
        2. Invalid command name
        3. Only command name passed
        4. End index is not passed
        5. Start index and end index is not passed
        6. End index is not an integer
        7. Start index is not an integer
        8. Extra params passed