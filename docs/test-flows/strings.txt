SET command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk write
        3. Invalid command name
        4. Only command name passed
        5. Key value pair incomplete

GET command:
    Syntax check flows:
        1. Correct syntax
        2. Bulk fetch
        3. Invalid command name
        4. Only command name passed